input {
    http {
        additional_codecs => {"Content-Type"=>"text/plain"}
        # request_headers_target_field => "application/logplex-1"
    }
}
filter {
    grok {
      match => {"message" => "%{GREEDYDATA:syslog_message}" }
    }
}
output {
    stdout { }
    # elasticsearch {
    #    hosts => "${HOST}"
    #    user => "${HOST_USER}"
    #    password => "${HOST_PASSWORD}"
    #    index => "heroku-logstash-%{+YYYY.MM.dd}"
    # }
}